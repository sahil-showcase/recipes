source "https://rubygems.org"

gem "jekyll", "~> 4.2"
gem "minima"       # or the default theme, if you’re just starting
gem "csv"
gem "logger"
gem "base64"
# If you are using Minimal Mistakes as a remote theme, you might not need to add it as a gem.
# If you want a local gem-based install, add:
# gem "minimal-mistakes-jekyll"

# GitHub Pages often uses a specific gem:
# gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end